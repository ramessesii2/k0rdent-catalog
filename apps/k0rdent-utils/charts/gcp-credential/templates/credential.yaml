apiVersion: k0rdent.mirantis.com/{{ $.Values.k0rdentApiVersion }}
kind: Credential
metadata:
  name: gcp-credential
spec:
  description: "GCP k0rdent credential"
  identityRef:
    apiVersion: v1
    kind: Secret
    name: gcp-cloud-sa
    namespace: {{ .Release.Namespace }}
