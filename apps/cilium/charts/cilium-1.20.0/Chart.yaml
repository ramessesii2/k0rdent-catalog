apiVersion: v2
name: cilium
description: A Helm chart that references the official "cilium" Helm chart.
type: application
version: 1.20.0
dependencies:
  - name: cilium
    version: 1.20.0
    repository: https://helm.cilium.io/
